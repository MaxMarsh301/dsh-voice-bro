/**
 * Voice surface plugin, node half. The browser implementation ships through
 * `exports["./client"]` and the `dsh.client` manifest.
 */

/** Host plugin body — this package contains browser behavior only. */
export function apply(): void {}
